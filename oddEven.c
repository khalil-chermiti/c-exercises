#include <stdio.h>

int main ()
{
    // finds out if number is odd or even
    int a ;
    printf("enter a number : ") ;
    scanf("%i" , &a) ;
    if(a / 2 == 0) printf("even !") ;
    else printf("odd !") ;
}
